# DEX iOS Port Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox syntax.

**Goal:** Daggerfall Enemy Expansion runs on iOS compiled in, full content, default-off, private draft only.
**Architecture:** WoD-family shape: upstream C# under `Ports/DEX/`, data from the `dex.dfmod` bundle via ModManager, boot-time launcher gate, RR-Items compat re-pointed at our port.
**Tech Stack:** DFU 1.1.1 fork, Unity 6000.3.23f1, IL2CPP iOS, Python pack tooling, MobileSelfTest.
**Spec:** `docs/superpowers/specs/2026-09-14-dex-port-design.md`.

## Global Constraints
- Branch `unity6-upgrade`; trailers `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01XFLMiLMMZbzbjem8vgUjYE`; files by name; revert scene/ProjectSettings/addressables churn.
- ONE Unity: `ps -Ao comm | grep -c "MacOS/Unity$"` == 0 before any run (never `pgrep -f`); kill nothing; never edit source while another agent's Unity run is in progress; MobileSelfTest.cs is edited by one agent at a time.
- Self-test: full app path, no `-nographics`; baseline = the last GREEN in the ledger. Tooling: `python3 -m unittest discover -s tools/bundled-mods -p "test_*.py"`; `fetch.py --check` 0 problems.
- TOKEN-LEAN: grep then ranged reads; one verification pass; one build per task that needs one; terse reports.
- Upstream pin: SquidKamer/DaggerfallBestiaryProject at the current default-branch head (Task 1 records the sha). Port headers: `// Ported from DEX 1.3.4 (Kab & Kamer, no licence declared - private draft only); every edit // MOBILE:`.
- Workspace `.superpowers/sdd/2026-09-14-dex-port/`.

### Task 1: Pack entry + fetch (no Unity)
mods.json `DEX`: repo, commit, manifest `DaggerfallBestiaryProject.dfmod.json`, `strip_code`, `private_only`, `pending:` (no LICENSE/header; art folders named after other Bethesda titles; permission sought from Kab and Kamer), `exclude_globs` `Editor/*` and anything the manifest does not list; check the manifest's `Files` covers CSVs, SpellRecords.json, modsettings.json, prefab and all textures (if it lists 8,000 paths the rewrite must keep them). Importer rule: sprites NOT readable, ASTC 6x6, alpha as the metas say; a per-mod rule only if defaults are wrong. `fetch.py`, `--check` 0 problems (53 mods), unittest OK, pack dry-run excludes DEX from public. Commit `Pack pipeline: fetch Daggerfall Enemy Expansion (private-only)`.

### Task 2: Port the code (inert) + gate
`Ports/DEX/` = all Scripts except Editor; `[Invoke]` removed; `DEXPort.Init(InitParams)`, `Installed`, `Available(bundleFound, csvsParsed)`; bundle mod resolved via ModManager by title; CSV loading unchanged (parse from the bundle's TextAssets); `SaveDataInterface` via the bundle mod as the C&C port does (else drop troll persistence, documented); `DEX_RRICompat` re-pointed at `Ports/RoleplayRealismItems` module flags with the 513-525 indices asserted against our RR-Items registration. Launcher: `Titles` += `Daggerfall Enemy Expansion`, default off, boot-time read, hint "restart required; saves with DEX enemies need it on". Self-tests: constants, id ranges from the shipped CSVs (50/43/39), RRI indices, no `[Invoke]`, title == manifest. Commit `Port sources: Daggerfall Enemy Expansion (inert; boot-time gate; RR-Items compat re-pointed)`.

### Task 3: Bundle build + test-app spawn command
Unscoped ReimportPacks + ApplyAll -> 53 bundles; `dfmod_inspect.py dex.dfmod` (8,031 textures ASTC, 6 CSVs, json, prefab; total size); backups 53x4; copy to `~/dev/dfu-mods/dex.dfmod`; upload `dex.dfmod`. `debug-newchar.txt` command `spawn <id|name>` (#if DFU_IOS_TESTAPP) using `GameObjectHelper.CreateEnemy` 3 m in front of the player; README-iOS debug section. Commit.

### Task 4: Simulator verification (one pass)
Builtin sim build, DEX on, summer 207,213; `spawn` three monsters (e.g. Goblin, Ogre, Dragon) and one class enemy; screenshot; log `[DEX] loaded ...`, per-archive first-import ms, no red errors; RR-Items on with DEX (compat path logs); negative run DEX off = vanilla tables. Restore 53x4 after.

### Task 5: Docs + device ipa
THIRD-PARTY.md (DEX entry: no licence, private only, art provenance), UPSTREAM-PATCHES.md (engine edits if any), README-iOS `### Daggerfall Enemy Expansion` (switch, restart rule, save rule, bundle, performance notes). `DFU-Test-unity6-dex.ipa` per the fog/autosave recipe (DFU_IOS_TESTAPP=1 DFU_BUNDLED_MODS=builtin, signed, bundle id pinned), upload, install if the iPad is available, push `dex.dfmod` to the container.

## Self-review
Spec 1 (T2), 2 (T2 data on), 3 (T2), 4 (T2), 5 (T1, T3), 6 (T2 tests, T3 spawn, T4, T5).
